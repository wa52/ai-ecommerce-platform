# Phase 12–15 增量验收证据

日期：2026-09-20

## 自动化结果

- `backend\.venv\Scripts\python -m pytest`：123 passed
- `frontend\npx tsc --noEmit`：exit 0
- `docker compose up -d --build storefront ai-backend`：构建和容器启动成功
- `GET http://127.0.0.1:3000/`：200
- `GET http://127.0.0.1:3000/account/addresses`：200
- `GET http://127.0.0.1:3000/favorites`：200
- `GET http://127.0.0.1:8001/openapi.json`：200

## Saleor GraphQL 验证

- `products { category { name slug } media { url alt } }`：返回真实商品分类和媒体。
- `checkoutAddPromoCode`：schema 和错误返回正常，假 checkout 返回 UUID 校验错误，不是 GraphQL 字段错误。
- `me { addresses { ... country { code country } } }`：匿名返回 `me=null`，查询字段验证通过。
- `categories(first: 5)`：返回真实分类数据。

## 代码范围

- Storefront：商品筛选/排序/详情图库/Variant/收藏/地址簿/优惠码。
- Admin：订单、库存、客户、分类、AI Center 页面。
- FastAPI：订单取消、标记支付、创建发货单；客户和分类列表。

## 未验证或阻塞

- 真实支付宝 Payment App、物流服务、售后退款服务、Shopify 真实店铺、真实 LLM 凭据未配置。
